/**
 * Rockell Tech background clips coding / infra only (Mixkit Free License).
 * Not the Key Partnership recruitment set.
 * @see https://mixkit.co/free-stock-video/coding/
 */

const mixkit = (id: number) => `https://assets.mixkit.co/videos/${id}/${id}-720.mp4`

/** Hero: IDE / code on screen dark, works with cyan overlay */
export const HERO_VIDEO = mixkit(1728)
/** Hero fallback: macro typing on keyboard */
export const HERO_VIDEO_FALLBACK = mixkit(1729)

/** Mid-page break: dual-monitor dev session “shipping in prod” */
export const BREAK_VIDEO = mixkit(41642)

/** Specialisms: hands on keyboard, fast iteration */
export const LANES_VIDEO = mixkit(41640)

/** Contact: laptop + code scroll human, still technical */
export const CONTACT_VIDEO = mixkit(1726)
